Block 1 of 3: Vertex Environment, Model Garden, and SFT
Colab setup, auth, Vertex AI, supervised tuning, Model Garden deployment and inference helpers.
# ============================================================# BLOCK 1 OF 3# ENVIRONMENT, COLAB AUTH, VERTEX AI, MODEL GARDEN, SFT SETUP# ============================================================# Purpose:# - Stable Colab dependencies# - Google Cloud authentication# - Vertex AI initialization# - Gemini supervised fine-tuning setup# - Vertex Model Garden deployment helpers for gpt-oss models# - Raw predict and OpenAI-compatible chat inference helpers## Security note:# - Do NOT hard-code API keys in notebooks.# - Use environment variables or Colab userdata/secrets.# ============================================================# ------------------------------------------------------------# 0. INSTALLS# ------------------------------------------------------------# Run once in Colab. These versions avoid the conflicts shown earlier:# - google-auth too old for Colab / ADK# - requests too old# - protobuf 6.x conflicts with generative language libraries# - numpy 2.4.x conflicts with TensorFlow/Numba# ------------------------------------------------------------%pip install -qU \  "google-cloud-aiplatform>=1.132.0" \  "google-auth>=2.47.0" \  "requests>=2.32.4" \  "openai>=1.64.0" \  "protobuf<6.0.0" \  "pydantic>=2.0,<=2.12.3" \  "python-docx" \  "pypdf" \  "numpy<2.2" \  "pandas" \  "scikit-learn" \  "beautifulsoup4" \  "wikipedia" \  "vaderSentiment" \  "sentence-transformers" \  "transformers" \  "accelerate" \  "bitsandbytes" \  "torch" \  "keras-nlp" \  "keras>=3" \  "model-alignment"# ------------------------------------------------------------# 1. IMPORTS# ------------------------------------------------------------from __future__ import annotationsimport osimport sysimport jsonimport mathimport timeimport plistlibimport requestsfrom pathlib import Pathfrom typing import Any, Dict, List, Optional, Unionimport numpy as npimport pandas as pdimport vertexaifrom vertexai.tuning import sftfrom vertexai.generative_models import GenerativeModeltry:    from vertexai import model_garden    from google.cloud import aiplatformexcept Exception as exc:    model_garden = None
    aiplatform = None    print(f"Model Garden imports unavailable: {exc}")try:    import google.auth    import google.auth.transport.requestsexcept Exception as exc:    google = None    print(f"Google auth imports unavailable: {exc}")try:    import openaiexcept Exception as exc:    openai = None    print(f"OpenAI SDK unavailable: {exc}")# ------------------------------------------------------------# 2. AUTHENTICATE NOTEBOOK ENVIRONMENT - COLAB ONLY# ------------------------------------------------------------if "google.colab" in sys.modules:    try:        from google.colab import auth        auth.authenticate_user()        print("Colab authentication complete.")    except Exception as exc:        print(f"Colab auth skipped or unsupported in this environment: {exc}")# ------------------------------------------------------------# 3. GOOGLE CLOUD PROJECT INFORMATION# ------------------------------------------------------------# Important corrections:# - Project IDs are usually lowercase. Example: "text-sentimentizer".# - Vertex AI expects region codes. Iowa = "us-central1", not "Iowa".# - Make sure Vertex AI API and billing are enabled.# ------------------------------------------------------------USER_PROJECT_ID = "text-sentimentizer"  # @param {type: "string"}USER_REGION = "us-central1"             # @param {type: "string"}PROJECT_ID = USER_PROJECT_ID or os.environ.get("GOOGLE_CLOUD_PROJECT")REGION = USER_REGION or os.environ.get("GOOGLE_CLOUD_REGION", "us-central1")if not PROJECT_ID:    raise ValueError("No Google Cloud project set. Set USER_PROJECT_ID or GOOGLE_CLOUD_PROJECT.")vertexai.init(project=PROJECT_ID, location=REGION)print(f"Project: {PROJECT_ID}")print(f"Location: {REGION}")# ------------------------------------------------------------# 4. GLOBAL VERTEX / GEMINI CONFIG# ------------------------------------------------------------SOURCE_MODEL = "gemini-1.5-pro-001"TUNED_MODEL_DISPLAY_NAME = "tuned-gemini-20260424-153536"RUN_VERTEX_SFT = FalseRUN_MODEL_GARDEN_DEPLOY = False# Vertex SFT requires GCS URIs, not local JSONL paths.GCS_TRAIN_DATASET_URI = "gs://your-bucket/neuropsych_sft_train.jsonl"GCS_VALIDATION_DATASET_URI = "gs://your-bucket/neuropsych_sft_validation.jsonl"# ------------------------------------------------------------# 5. VERTEX SUPERVISED FINE-TUNING HELPER# ------------------------------------------------------------class VertexSFTTrainer:    """Starts a Vertex AI supervised fine-tuning job."""    @staticmethod    def start(        train_dataset_uri: str,        validation_dataset_uri: str,
        source_model: str = SOURCE_MODEL,        tuned_model_display_name: str = TUNED_MODEL_DISPLAY_NAME,        epochs: int = 4,        learning_rate_multiplier: float = 1.0,        adapter_size: int = 8,    ):        vertexai.init(project=PROJECT_ID, location=REGION)        job = sft.train(            source_model=source_model,            train_dataset=train_dataset_uri,            validation_dataset=validation_dataset_uri,            tuned_model_display_name=tuned_model_display_name,            epochs=epochs,            learning_rate_multiplier=learning_rate_multiplier,            adapter_size=adapter_size,        )        print(f"Tuning job started: {job.resource_name}")        return job# Optional exact high-intensity retry configuration from earlier notebook.def start_high_intensity_sft_retry():    return VertexSFTTrainer.start(        train_dataset_uri="gs://cloud-samples-data/ai-platform/generative_ai/gemini-2_0/text/sft_train_data.jsonl",        validation_dataset_uri="gs://cloud-samples-data/ai-platform/generative_ai/gemini-2_0/text/sft_validation_data.jsonl",        source_model="gemini-1.5-pro-001",        tuned_model_display_name=TUNED_MODEL_DISPLAY_NAME,        epochs=16,        learning_rate_multiplier=2.0,        adapter_size=16,    )# ------------------------------------------------------------# 6. MODEL GARDEN MODEL VARIANT SELECTION# ------------------------------------------------------------model_version = "gpt-oss-120b"  # @param ["gpt-oss-120b", "gpt-oss-20b"]MODEL_NAME = f"openai/gpt-oss@{model_version}"use_dedicated_endpoint = Trueendpoints: Dict[str, Any] = {}endpoint = NoneLABEL = None# ------------------------------------------------------------# 7. VERTEX MODEL GARDEN DEPLOYMENT + INFERENCE MANAGER# ------------------------------------------------------------class VertexModelGardenManager:    """    Deploys and queries open models from Vertex AI Model Garden.    Deployment can be expensive. Keep RUN_MODEL_GARDEN_DEPLOY=False until ready.    """    @staticmethod    def list_deployable_gpt_oss_models() -> list:        if model_garden is None:            print("vertexai.model_garden is unavailable.")            return []        all_model_versions = model_garden.list_deployable_models(            model_filter="gpt-oss",            list_hf_models=False,        )        print("Deployable gpt-oss model variants:")        for item in all_model_versions:            print(item)        return all_model_versions    @staticmethod    def initialize_open_model(model_name: str = MODEL_NAME):        if model_garden is None:            raise RuntimeError("vertexai.model_garden is unavailable.")
        model = model_garden.OpenModel(model_name)        print(f"Initialized Model Garden model: {model_name}")        return model    @staticmethod    def list_deploy_options(model_name: str = MODEL_NAME):        model = VertexModelGardenManager.initialize_open_model(model_name)        deploy_options = model.list_deploy_options(concise=True)        print(deploy_options)        return deploy_options    @staticmethod    def deploy_default(model_name: str = MODEL_NAME, use_dedicated_endpoint: bool = True):        model = VertexModelGardenManager.initialize_open_model(model_name)        deployed_endpoint = model.deploy(            accept_eula=True,            use_dedicated_endpoint=use_dedicated_endpoint,        )        print(f"Default endpoint deployed: {deployed_endpoint.resource_name}")        return deployed_endpoint    @staticmethod    def deploy_custom_h100(model_name: str = MODEL_NAME, use_dedicated_endpoint: bool = True):        model = VertexModelGardenManager.initialize_open_model(model_name)        deployed_endpoint = model.deploy(            accept_eula=True,            use_dedicated_endpoint=use_dedicated_endpoint,            serving_container_image_uri=(                "us-docker.pkg.dev/deeplearning-platform-release/"                "vertex-model-garden/sglang-serve.cu124.0-4.ubuntu2204.py310:"                "model-garden.sglang-0-4-release_20250831.00_p0"            ),            machine_type="a3-highgpu-8g",            accelerator_type="NVIDIA_H100_80GB",            accelerator_count=8,        )        print(f"Custom H100 endpoint deployed: {deployed_endpoint.resource_name}")        return deployed_endpoint    @staticmethod    def select_endpoint(endpoints: dict):        if "sdk_default" in endpoints:            return endpoints["sdk_default"], "sdk_default"        if "sdk_custom" in endpoints:            return endpoints["sdk_custom"], "sdk_custom"        raise ValueError("No endpoint found. Create an endpoint first.")    @staticmethod    def load_existing_endpoint(endpoint_name: str):        if aiplatform is None:            raise RuntimeError("google.cloud.aiplatform is unavailable.")        if endpoint_name.startswith("projects/"):            aip_endpoint_name = endpoint_name        else:            aip_endpoint_name = f"projects/{PROJECT_ID}/locations/{REGION}/endpoints/{endpoint_name}"        loaded_endpoint = aiplatform.Endpoint(aip_endpoint_name)        print(f"Loaded endpoint: {loaded_endpoint.resource_name}")        return loaded_endpoint    @staticmethod    def raw_predict(        endpoint,        prompt: str = "What is a car?",        max_tokens: int = 50,        temperature: float = 1.0,        top_p: float = 1.0,        top_k: int = 1,        raw_response: bool = False,        use_dedicated_endpoint: bool = True,    ):        instances = [            {                "prompt": prompt,                "max_tokens": max_tokens,                "temperature": temperature,                "top_p": top_p,                "top_k": top_k,                "raw_response": raw_response,
            }        ]        response = endpoint.predict(            instances=instances,            use_dedicated_endpoint=use_dedicated_endpoint,        )        for prediction in response.predictions:            print(prediction)        return response    @staticmethod    def delete_endpoints(endpoints: dict) -> None:        for label, endpoint_obj in endpoints.items():            print(f"Deleting endpoint: {label}")            endpoint_obj.delete(force=True)def vertex_openai_chat_completion(    endpoint,    user_message: str = "How is your day going?",    max_tokens: int = 50,    temperature: float = 1.0,    stream: bool = False,    use_dedicated_endpoint: bool = True,):    """Sends chat-completion requests to a deployed Vertex endpoint using OpenAI SDK."""    if openai is None:        raise RuntimeError("OpenAI SDK is unavailable.")    creds, _ = google.auth.default(scopes=["https://www.googleapis.com/auth/cloud-platform"])    auth_req = google.auth.transport.requests.Request()    creds.refresh(auth_req)    endpoint_resource_name = endpoint.resource_name    base_url = f"https://{REGION}-aiplatform.googleapis.com/v1beta1/{endpoint_resource_name}"    if use_dedicated_endpoint:        try:            dedicated_dns = endpoint.gca_resource.dedicated_endpoint_dns            base_url = f"https://{dedicated_dns}/v1beta1/{endpoint_resource_name}"        except Exception as exc:            print(f"Dedicated endpoint DNS unavailable; using regional endpoint. {exc}")    client = openai.OpenAI(base_url=base_url, api_key=creds.token)    model_response = client.chat.completions.create(        model="",        messages=[{"role": "user", "content": user_message}],        temperature=temperature,        max_tokens=max_tokens,        stream=stream,    )    if stream:        usage = None        contents = []        for chunk in model_response:            if getattr(chunk, "usage", None) is not None:                usage = chunk.usage                continue            delta = chunk.choices[0].delta.content or ""            print(delta, end="", flush=True)            contents.append(delta)        print(f"\n\nUsage: {usage}")        return "".join(contents)    print(model_response)    return model_responsedef model_garden_demo() -> None:    """Optional deployment workflow. Disabled unless RUN_MODEL_GARDEN_DEPLOY=True."""    if not RUN_MODEL_GARDEN_DEPLOY:        print("RUN_MODEL_GARDEN_DEPLOY=False, so no endpoint was deployed.")        return
    VertexModelGardenManager.list_deployable_gpt_oss_models()    model = VertexModelGardenManager.initialize_open_model(MODEL_NAME)    deploy_options = model.list_deploy_options(concise=True)    print(deploy_options)    endpoints["sdk_default"] = model.deploy(        accept_eula=True,        use_dedicated_endpoint=use_dedicated_endpoint,    )    selected_endpoint, selected_label = VertexModelGardenManager.select_endpoint(endpoints)    print(f"Selected endpoint: {selected_label}")    VertexModelGardenManager.raw_predict(        endpoint=selected_endpoint,        prompt="What is a car?",        max_tokens=50,        temperature=1.0,        top_p=1.0,        top_k=1,        raw_response=False,        use_dedicated_endpoint=use_dedicated_endpoint,    )    vertex_openai_chat_completion(        endpoint=selected_endpoint,        user_message="How is your day going?",        max_tokens=50,        temperature=1.0,        stream=False,        use_dedicated_endpoint=use_dedicated_endpoint,    )    # Cleanup is intentionally disabled by default.    # VertexModelGardenManager.delete_endpoints(endpoints)if __name__ == "__main__":    print("Block 1 loaded. Set flags and call model_garden_demo() or VertexSFTTrainer.start(...).")