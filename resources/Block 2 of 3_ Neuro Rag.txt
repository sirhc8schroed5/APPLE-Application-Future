Block 2 of 3: Neuropsych RAG, Scoring, Reporting, and PDF Revision
Document extraction, RAG, SFT JSONL, scoring, domain mapping, report generation and PDF revision.
# ============================================================# BLOCK 2 OF 3# NEUROPSYCHOLOGICAL RAG, SFT DATASET, SCORING, DOMAIN MAPPING,# VERDANA/KAMILLA REPORT REVISION, DOCX EXPORT# ============================================================# This block is a consolidated Python module for:# - authorized document extraction (.docx, .webloc, .pdf)# - public measure scraping from Wikipedia# - RAG indexing# - local JSONL generation for SFT/RAG review# - safe scoring scaffolds# - human sentiment and validity checks# - multimodal data ingestion and domain routing# - clinical prompt generation# - Word report export# - optional Verdana/Kamilla PDF report revisionfrom __future__ import annotationsimport osimport reimport jsonimport mathimport plistlibimport requestsfrom pathlib import Pathfrom typing import Any, Dict, List, Optional, Unionimport numpy as npimport pandas as pdimport wikipediaimport pypdffrom pydantic import BaseModel, Fieldfrom docx import Documentfrom docx.shared import Ptfrom sklearn.feature_extraction.text import TfidfVectorizerfrom sklearn.metrics.pairwise import cosine_similarityfrom vaderSentiment.vaderSentiment import SentimentIntensityAnalyzerfrom vertexai.generative_models import GenerativeModelCONTENT_DIR = Path("/content")AUTHORIZED_SOURCE_DIR = CONTENT_DIR / "authorized_sources"OUTPUT_DIR = CONTENT_DIR / "neuropsych_outputs"DOCX_PATH = CONTENT_DIR / "Py Amalgam Herman.docx"WEBLOC_PATH = CONTENT_DIR / "Download TAT Interpretive Strategy.docx.webloc"TRAIN_JSONL = OUTPUT_DIR / "neuropsych_sft_train.jsonl"VALIDATION_JSONL = OUTPUT_DIR / "neuropsych_sft_validation.jsonl"REPORT_DOCX = OUTPUT_DIR / "neuropsych_report_draft.docx"VERDANA_REPORT_PDF = CONTENT_DIR / "Verdana's Report.pdf"BACKGROUND_INFO_PDF = CONTENT_DIR / "Further Background Information.pdf"EDITING_PATTERNS_PDF = CONTENT_DIR / "Patterns of editing within Sarah B.’s revisions.pdf"RR_I_PDF = CONTENT_DIR / "RR I.pdf"VERDANA_OUTPUT_DOCX = CONTENT_DIR / "Verdana_Revised_Report_Final.docx"SOURCE_MODEL = "gemini-1.5-pro-001"RUN_PUBLIC_WIKIPEDIA_SCRAPE = TrueRUN_GEMINI_REPORT_REVISION = FalseAUTHORIZED_SOURCE_DIR.mkdir(parents=True, exist_ok=True)OUTPUT_DIR.mkdir(parents=True, exist_ok=True)class ScoreEntry(BaseModel):    test_name: str    subtest_name: Optional[str] = None    raw_score: Optional[float] = None    scaled_score: Optional[float] = None    standard_score: Optional[float] = None    t_score: Optional[float] = None    percentile: Optional[float] = None    qualitative_label: Optional[str] = None    age: Optional[float] = None    gender: Optional[str] = None    notes: Optional[str] = Noneclass ReportInput(BaseModel):    client_name: str
    age: float    referral_question: str    scores: List[ScoreEntry]    background_summary: Optional[str] = None    behavioral_observations: Optional[str] = None    diagnostic_questions: List[str] = Field(default_factory=list)class SourceChunk(BaseModel):    source_name: str    text: str    metadata: Dict[str, Any] = Field(default_factory=dict)class ClinicalFinding(BaseModel):    domain: str    finding: str    evidence: List[str] = Field(default_factory=list)    confidence: str = "moderate"    cautions: List[str] = Field(default_factory=list)class DocumentExtractor:    @staticmethod    def read_docx(path: Union[str, Path]) -> str:        path = Path(path)        doc = Document(path)        parts = []        for para in doc.paragraphs:            text = para.text.strip()            if text:                parts.append(text)        for table in doc.tables:            for row in table.rows:                cells = [cell.text.strip() for cell in row.cells]                if any(cells):                    parts.append(" | ".join(cells))        return "\n".join(parts)    @staticmethod    def read_pdf(path: Union[str, Path]) -> str:        path = Path(path)        if not path.exists():            return f"[FILE NOT FOUND: {path}]"        try:            reader = pypdf.PdfReader(str(path))            return "\n".join([(page.extract_text() or "") for page in reader.pages]).strip()        except Exception as exc:            return f"[PDF READ ERROR: {path}: {exc}]"    @staticmethod    def read_webloc(path: Union[str, Path]) -> Optional[str]:        path = Path(path)        try:            with path.open("rb") as f:                data = plistlib.load(f)            return data.get("URL")        except Exception:            text = path.read_text(encoding="utf-8", errors="ignore")            match = re.search(r"https?://[^\s<>\"]+", text)            return match.group(0) if match else None    @staticmethod    def download_docx(url: str, output_path: Union[str, Path]) -> Optional[Path]:        output_path = Path(output_path)        try:            response = requests.get(url, timeout=30)            response.raise_for_status()            if "html" in response.headers.get("content-type", "").lower():                print(f"Download returned HTML/login page, not DOCX: {url}")                return None            output_path.write_bytes(response.content)            return output_path        except Exception as exc:            print(f"Download failed: {exc}")            return None    @classmethod    def extract_local_sources(cls, output_dir: Union[str, Path]) -> List[Path]:        output_dir = Path(output_dir)        output_dir.mkdir(parents=True, exist_ok=True)        extracted = []        if DOCX_PATH.exists():
            text = cls.read_docx(DOCX_PATH)            out = output_dir / f"{DOCX_PATH.stem}.txt"            out.write_text(text, encoding="utf-8")            extracted.append(out)            print(f"Extracted DOCX -> {out}")        else:            print(f"Missing DOCX: {DOCX_PATH}")        if WEBLOC_PATH.exists():            url = cls.read_webloc(WEBLOC_PATH)            print(f"WEBLOC URL: {url}")            if url:                downloaded = cls.download_docx(url, output_dir / "downloaded_interpretive_strategy.docx")                if downloaded:                    text = cls.read_docx(downloaded)                    out = output_dir / "downloaded_interpretive_strategy.txt"                    out.write_text(text, encoding="utf-8")                    extracted.append(out)        return extracteddef extract_text_from_pdf(file_path: Union[str, Path]) -> str:    return DocumentExtractor.read_pdf(file_path)class PublicMeasureScraper:    MEASURES = {        "WAIS-IV": "Wechsler Adult Intelligence Scale",        "PAI": "Personality Assessment Inventory",        "R-PAS": "Rorschach Performance Assessment System",        "BRIEF-2": "Behavior Rating Inventory of Executive Function",        "TOVA": "Test of Variables of Attention",        "WRAML": "Wide Range Assessment of Memory and Learning",        "CVLT-3": "California Verbal Learning Test",        "PHQ-9": "Patient Health Questionnaire",        "TSI-2": "Trauma Symptom Inventory",    }    @classmethod    def scrape_wikipedia(cls) -> Dict[str, str]:        scraped = {}        for abbr, query in cls.MEASURES.items():            try:                page = wikipedia.page(query, auto_suggest=False)                scraped[abbr] = page.content                print(f"Scraped {abbr}: {len(page.content)} characters")            except Exception as exc:                print(f"Could not scrape {abbr}: {exc}")        return scraped    @classmethod    def save_scraped_data(cls, output_dir: Union[str, Path]) -> List[Path]:        output_dir = Path(output_dir)        output_dir.mkdir(parents=True, exist_ok=True)        paths = []        for abbr, text in cls.scrape_wikipedia().items():            path = output_dir / f"public_{abbr}.txt"            path.write_text(text, encoding="utf-8")            paths.append(path)        return pathsclass SourceLibrary:    def __init__(self) -> None:        self.chunks: List[SourceChunk] = []        self.vectorizer = TfidfVectorizer(stop_words="english", max_features=30000)        self.matrix = None    def add_text(self, source_name: str, text: str, chunk_size: int = 1400, overlap: int = 180) -> None:        text = re.sub(r"\s+", " ", text).strip()        start = 0        idx = 0        while start < len(text):            end = min(start + chunk_size, len(text))            self.chunks.append(SourceChunk(source_name=source_name, text=text[start:end], metadata={"chunk_index": idx}))            if end >= len(text):                break            start = max(0, end - overlap)            idx += 1    def load_dir(self, source_dir: Union[str, Path]) -> None:        source_dir = Path(source_dir)        for path in source_dir.glob("*.txt"):            self.add_text(path.name, path.read_text(encoding="utf-8", errors="ignore"))        if self.chunks:            self.build_index()    def build_index(self) -> None:        self.matrix = self.vectorizer.fit_transform([c.text for c in self.chunks])
    def retrieve(self, query: str, top_k: int = 8) -> List[SourceChunk]:        if not self.chunks:            return []        if self.matrix is None:            self.build_index()        q = self.vectorizer.transform([query])        sims = cosine_similarity(q, self.matrix)[0]        idx = np.argsort(sims)[::-1][:top_k]        return [self.chunks[i] for i in idx if sims[i] > 0]class SFTDatasetBuilder:    @staticmethod    def make_example(chunk: SourceChunk) -> Dict[str, Any]:        return {"messages": [            {"role": "user", "content": "Extract clinically useful public-domain or authorized information from this source. Do not reproduce proprietary manual text.\n\nSOURCE:\n" + chunk.text},            {"role": "model", "content": "Identify relevant constructs, task/symptom domain, public score structure if applicable, interpretive cautions, and limits of inference. Do not diagnose from a single score."},        ]}    @classmethod    def build_jsonl(cls, library: SourceLibrary, train_path: Union[str, Path], validation_path: Union[str, Path], validation_ratio: float = 0.15) -> None:        train_path = Path(train_path)        validation_path = Path(validation_path)        train_path.parent.mkdir(parents=True, exist_ok=True)        examples = [cls.make_example(c) for c in library.chunks]        if not examples:            print("No chunks available for SFT JSONL.")            return        rng = np.random.default_rng(seed=42)        rng.shuffle(examples)        n_val = max(1, int(len(examples) * validation_ratio))        validation = examples[:n_val]        train = examples[n_val:]        with train_path.open("w", encoding="utf-8") as f:            for row in train:                f.write(json.dumps(row, ensure_ascii=False) + "\n")        with validation_path.open("w", encoding="utf-8") as f:            for row in validation:                f.write(json.dumps(row, ensure_ascii=False) + "\n")        print(f"Train JSONL: {train_path} ({len(train)} examples)")        print(f"Validation JSONL: {validation_path} ({len(validation)} examples)")class NormInterpreter:    @staticmethod    def normal_cdf_percentile(value: float, mean: float, sd: float) -> float:        z = (value - mean) / sd        return round(float(0.5 * (1 + math.erf(z / math.sqrt(2))) * 100), 2)    @staticmethod    def standard_score_to_percentile(score: Optional[float]) -> Optional[float]:        return None if score is None else NormInterpreter.normal_cdf_percentile(score, 100, 15)    @staticmethod    def t_score_to_percentile(t_score: Optional[float]) -> Optional[float]:        return None if t_score is None else NormInterpreter.normal_cdf_percentile(t_score, 50, 10)    @staticmethod    def label_standard_score(score: Optional[float]) -> Optional[str]:        if score is None: return None        if score >= 130: return "exceptionally high"        if score >= 120: return "very high"        if score >= 110: return "high average"        if score >= 90: return "average"        if score >= 80: return "low average"        if score >= 70: return "very low"        return "exceptionally low"    @staticmethod    def label_scaled_score(score: Optional[float]) -> Optional[str]:        if score is None: return None        if score >= 16: return "exceptionally high"        if score >= 14: return "very high"        if score >= 12: return "high average"        if score >= 8: return "average"        if score >= 6: return "low average"        if score >= 4: return "very low"        return "exceptionally low"    @classmethod    def enrich_scores(cls, scores: List[ScoreEntry]) -> List[ScoreEntry]:        enriched = []        for s in scores:            update = {}            if s.percentile is None:                if s.standard_score is not None: update["percentile"] = cls.standard_score_to_percentile(s.standard_score)                elif s.t_score is not None: update["percentile"] = cls.t_score_to_percentile(s.t_score)            if s.qualitative_label is None:
                if s.standard_score is not None: update["qualitative_label"] = cls.label_standard_score(s.standard_score)                elif s.scaled_score is not None: update["qualitative_label"] = cls.label_scaled_score(s.scaled_score)            enriched.append(s.model_copy(update=update))        return enrichedclass WAISScorer:    def __init__(self, norm_table_csv: Optional[Union[str, Path]] = None):        self.norms = pd.read_csv(norm_table_csv) if norm_table_csv else None    def calculate_composite_from_authorized_table(self, index_name: str, sum_scaled_scores: float, age: Optional[float] = None) -> Optional[float]:        if self.norms is None:            return None        df = self.norms.copy()        df = df[df["index_name"].str.lower() == index_name.lower()]        df = df[df["sum_scaled_scores"] == sum_scaled_scores]        if age is not None and {"age_min", "age_max"}.issubset(df.columns):            df = df[(df["age_min"] <= age) & (df["age_max"] >= age)]        return None if df.empty else float(df.iloc[0]["standard_score"])    def mock_composite_for_testing(self, index_name: str, sum_scaled_scores: float) -> Optional[float]:        mock_rules = {"VCI": lambda x: x * 2 + 50, "PRI": lambda x: x * 2 + 50, "WMI": lambda x: x * 2 + 50, "PSI": lambda x: x * 2 + 50, "FSIQ": lambda x: x * 1.5 + 40}        return float(mock_rules[index_name](sum_scaled_scores)) if index_name in mock_rules else Noneclass WAISInterpreter:    def get_descriptor(self, score: Optional[float]) -> str:        return NormInterpreter.label_standard_score(score) or "unknown"    def generate_narrative(self, scores: Dict[str, Optional[float]]) -> str:        lines = ["Clinical Narrative:"]        strengths, weaknesses = [], []        for index, score in scores.items():            if score is None:                lines.append(f"The {index} score could not be calculated from available data.")                continue            desc = self.get_descriptor(score)            lines.append(f"The {index} score is {score:.1f}, which falls in the {desc} range.")            if score >= 110: strengths.append(index)            elif score < 90: weaknesses.append(index)        lines.append("")        lines.append("Cognitive strengths: " + (", ".join(strengths) if strengths else "none automatically flagged."))        lines.append("Cognitive weaknesses: " + (", ".join(weaknesses) if weaknesses else "none automatically flagged."))        return "\n".join(lines)class HumanSentimentAgent:    def __init__(self):        self.analyzer = SentimentIntensityAnalyzer()    def analyze(self, text: str) -> Dict[str, Any]:        scores = self.analyzer.polarity_scores(text or "")        compound = scores["compound"]        tone = "resilient / hopeful" if compound >= 0.35 else "distressed / strained" if compound <= -0.35 else "mixed / reflective"        return {"tone": tone, "scores": scores, "interpretive_note": "Sentiment is contextual and should not be treated as diagnosis."}class ValidityChecker:    @staticmethod    def check(scores: List[ScoreEntry], observations: Optional[str]) -> List[str]:        cautions = []        if not scores: cautions.append("No scores were provided.")        if observations and any(term in observations.lower() for term in ["fatigue", "invalid", "poor effort", "distractible"]):            cautions.append("Behavioral observations include factors that may affect validity.")        return cautionsclass DataIngestor:    def __init__(self):        self.data_store = {"quantitative_scores": {}, "qualitative_observations": {}, "patient_history": {}, "source_documents": {}}    def ingest_csv(self, file_path: Union[str, Path], category: str, key_name: str) -> None:        file_path = Path(file_path)        if not file_path.exists():            print(f"File not found: {file_path}")            return        if category not in self.data_store: raise ValueError(f"Invalid category: {category}")        self.data_store[category][key_name] = pd.read_csv(file_path)    def ingest_json(self, file_path: Union[str, Path], category: str, key_name: str) -> None:        file_path = Path(file_path)        if not file_path.exists():            print(f"File not found: {file_path}")            return        if category not in self.data_store: raise ValueError(f"Invalid category: {category}")        with file_path.open("r", encoding="utf-8") as f:            self.data_store[category][key_name] = json.load(f)    def ingest_text(self, text: str, category: str, key_name: str) -> None:        if category not in self.data_store: raise ValueError(f"Invalid category: {category}")        self.data_store[category][key_name] = text    def get_unified_data(self) -> Dict[str, Dict[str, Any]]:
        return self.data_storeclass DomainMapper:    def __init__(self, mapping_rules: Optional[Dict[str, List[str]]] = None):        self.mapping_rules = mapping_rules or {"wisc_v": ["cognitive", "executive_functioning"], "wais_iv": ["cognitive", "executive_functioning"], "d_kefs": ["executive_functioning", "cognitive"], "cvlt_3": ["learning_memory", "cognitive"], "brief_2": ["executive_functioning", "social"], "vanderbilt": ["executive_functioning", "social"], "background": ["social", "emotional", "developmental"], "observations": ["emotional", "social", "cognitive"], "report_text": ["cognitive", "emotional", "executive_functioning", "social", "developmental"]}        self.domain_data = {"cognitive": {}, "emotional": {}, "executive_functioning": {}, "social": {}, "developmental": {}, "learning_memory": {}, "daily_living": {}, "academic_occupational": {}}    def map_data(self, unified_data: Dict[str, Dict[str, Any]]) -> Dict[str, Dict[str, Any]]:        for category, items in unified_data.items():            for key_name, data in items.items():                for domain in self.mapping_rules.get(key_name, []):                    self.domain_data.setdefault(domain, {}).setdefault(category, {})[key_name] = data        return self.domain_datadef to_jsonable(obj: Any) -> Any:    if isinstance(obj, pd.DataFrame): return obj.to_dict(orient="records")    if isinstance(obj, dict): return {k: to_jsonable(v) for k, v in obj.items()}    if isinstance(obj, list): return [to_jsonable(v) for v in obj]    return objclass DiagnosticRouter:    def __init__(self):        self.diagnostic_keywords = {"F88 Other Specified Neurodevelopmental Disorder": ["neurodevelopmental", "developmental", "executive", "social communication", "masking", "adaptive", "cognitive flexibility"], "F32.89 Other Specified Depressive Disorder": ["depressive", "mood", "sadness", "hopelessness", "anhedonia", "fatigue", "irritability"], "F41.8 Other Specified Anxiety Disorder": ["anxiety", "worry", "nervous", "panic", "avoidance", "fear", "hypervigilance"]}        self.impairment_keywords = {"social": ["social", "interaction", "relationship", "communication", "peer"], "academic_occupational": ["work", "job", "school", "academic", "occupational"], "daily_living": ["daily life", "routine", "self-care", "activities of daily living"], "executive": ["planning", "organization", "attention", "initiation", "flexibility"]}    def parse_and_route(self, text: str) -> Dict[str, List[str]]:        text_lower = text.lower()        routed = {"potential_diagnoses_for_clinician_review": [], "functional_impairments_for_review": []}        for diagnosis, keywords in self.diagnostic_keywords.items():            if any(keyword in text_lower for keyword in keywords): routed["potential_diagnoses_for_clinician_review"].append(diagnosis)        for impairment, keywords in self.impairment_keywords.items():            if any(keyword in text_lower for keyword in keywords): routed["functional_impairments_for_review"].append(impairment)        return routedclass NeuropsychRuleEngine:    DOMAIN_KEYWORDS = {"Executive Functioning": ["D-KEFS", "Trail Making", "Stroop", "inhibition", "switching", "planning"], "Working Memory": ["Digit Span", "Picture Span", "Letter-Number", "working memory"], "Processing Speed": ["Coding", "Symbol Search", "Cancellation", "processing speed"], "Verbal Reasoning": ["Similarities", "Vocabulary", "Information", "Comprehension"], "Visual-Spatial / Fluid Reasoning": ["Block Design", "Matrix Reasoning", "Visual Puzzles", "Figure Weights"], "Learning and Memory": ["CVLT", "WMS", "learning", "recall", "recognition"]}    def infer_domains(self, report_input: ReportInput) -> List[ClinicalFinding]:        findings = []        for domain, keywords in self.DOMAIN_KEYWORDS.items():            matched = [s for s in report_input.scores if any(k.lower() in f"{s.test_name} {s.subtest_name or ''} {s.notes or ''}".lower() for k in keywords)]            if not matched: continue            evidence, low, high = [], 0, 0            for s in matched:                label = s.qualitative_label or "unlabeled"                evidence.append(f"{s.test_name}{' - ' + s.subtest_name if s.subtest_name else ''}: {label}")                low += int(label in {"low average", "very low", "exceptionally low"})                high += int(label in {"high average", "very high", "exceptionally high"})            text = f"Results suggest convergent vulnerability in {domain.lower()}." if low >= 2 else f"Results suggest circumscribed weakness or variability in {domain.lower()}." if low == 1 else f"Results suggest relative strength in {domain.lower()}." if high >= 1 else f"Results fall broadly within expected limits for {domain.lower()}."            findings.append(ClinicalFinding(domain=domain, finding=text, evidence=evidence, confidence="moderate" if len(matched) >= 2 else "low", cautions=["Do not diagnose from isolated scores.", "Integrate history, behavior, validity, and functional impairment."]))        return findingsclass ClinicalPromptEngine:    def __init__(self, model_name: str = SOURCE_MODEL):        self.model = GenerativeModel(model_name)    def build_prompt(self, domain_data: Dict[str, Any]) -> str:        return f"""Act as an expert clinical neuropsychology drafting assistant.Domain Data:{json.dumps(to_jsonable(domain_data), indent=2, ensure_ascii=False)}Rules: avoid premature conceptualization; do not diagnose from isolated scores; connect domains; distinguish observation from interpretation; require clinician review.""".strip()    def generate_interpretation(self, prompt: str) -> str:        try: return self.model.generate_content(prompt).text        except Exception as exc: return f"Error generating interpretation: {exc}"class ClinicalPromptBuilder:    @staticmethod    def build(report_input: ReportInput, findings: List[ClinicalFinding], retrieved_sources: List[SourceChunk], validity_cautions: List[str], sentiment: Dict[str, Any]) -> str:        source_text = "\n\n".join(f"[{c.source_name} | chunk {c.metadata.get('chunk_index')}]\n{c.text}" for c in retrieved_sources)        return f"""You are drafting a neuropsychological report section.Rules: cautious clinical prose; do not diagnose from scores alone; link task demand -> cognitive process -> observed performance -> functional implication; avoid copying source text verbatim.Client: {report_input.model_dump_json(indent=2)}Findings: {json.dumps([f.model_dump() for f in findings], indent=2)}Validity cautions: {json.dumps(validity_cautions, indent=2)}Sentiment/context note: {json.dumps(sentiment, indent=2)}Retrieved authorized/public context: {source_text}""".strip()class LocalClinicalWriter:    def generate(self, report_input: ReportInput, findings: List[ClinicalFinding], validity_cautions: List[str], sentiment: Dict[str, Any]) -> str:        lines = ["Revision Process Overview", "This draft integrates test scores, score descriptors, behavioral observations, public or authorized source material, and rule-based clinical cautions into a structured interpretive framework.", "", "Neuropsychological Findings"]
        for f in findings:            lines += ["", f.domain, f.finding, "Evidence:"] + [f"- {ev}" for ev in f.evidence] + ["Cautions:"] + [f"- {c}" for c in f.cautions]        lines += ["", "Affective / Behavioral Context", f"The observed language tone was classified as {sentiment.get('tone', 'not available')}. This is contextual information only and should not be interpreted diagnostically.", "", "Diagnostic Integration", "Diagnostic conclusions require integration of developmental history, symptom course, impairment, test validity, behavioral observations, and multi-method data.", "", "Interpretive Cautions"]        lines += [f"- {c}" for c in validity_cautions] if validity_cautions else ["- No automated validity cautions were triggered, though clinician review remains required."]        lines += ["", "Clinician Review Summary", "This automated draft is not a final clinical report. A qualified clinician must verify score conversions, norm applicability, diagnostic reasoning, and final wording."]        return "\n".join(lines)class WordReportExporter:    @staticmethod    def export(report_text: str, output_path: Union[str, Path]) -> None:        output_path = Path(output_path)        output_path.parent.mkdir(parents=True, exist_ok=True)        doc = Document()        doc.styles["Normal"].font.name = "Times New Roman"        doc.styles["Normal"].font.size = Pt(11)        doc.add_heading("Clinical Psychological / Neuropsychological Report Draft", level=1)        headings = {"Revision Process Overview", "Neuropsychological Findings", "Affective / Behavioral Context", "Diagnostic Integration", "Interpretive Cautions", "Clinician Review Summary"}        for line in report_text.splitlines():            clean = line.strip()            if not clean: doc.add_paragraph("")            elif clean in headings: doc.add_heading(clean, level=2)            elif clean in NeuropsychRuleEngine.DOMAIN_KEYWORDS: doc.add_heading(clean, level=3)            else: doc.add_paragraph(clean)        doc.save(output_path)        print(f"Saved report: {output_path}")class NeuropsychReportEngine:    def __init__(self, source_dir: Optional[Union[str, Path]] = None):        self.library = SourceLibrary(); self.rules = NeuropsychRuleEngine(); self.writer = LocalClinicalWriter(); self.sentiment_agent = HumanSentimentAgent()        if source_dir: self.library.load_dir(source_dir)    def run(self, report_input: ReportInput, output_docx: Union[str, Path]) -> str:        report_input = report_input.model_copy(update={"scores": NormInterpreter.enrich_scores(report_input.scores)})        validity_cautions = ValidityChecker.check(report_input.scores, report_input.behavioral_observations)        findings = self.rules.infer_domains(report_input)        query = " ".join([report_input.referral_question, " ".join([f"{s.test_name} {s.subtest_name or ''}" for s in report_input.scores]), " ".join(report_input.diagnostic_questions)])        retrieved = self.library.retrieve(query, top_k=8)        sentiment = self.sentiment_agent.analyze(report_input.behavioral_observations or "")        prompt = ClinicalPromptBuilder.build(report_input, findings, retrieved, validity_cautions, sentiment)        report_text = self.writer.generate(report_input, findings, validity_cautions, sentiment)        WordReportExporter.export(report_text, output_docx)        Path(output_docx).with_suffix(".prompt.txt").write_text(prompt, encoding="utf-8")        return report_textclass VerdanaReportRevisionPipeline:    def __init__(self, model_name: str = SOURCE_MODEL): self.model = GenerativeModel(model_name)    @staticmethod    def load_source_texts() -> Dict[str, str]:        return {"verdana_report": DocumentExtractor.read_pdf(VERDANA_REPORT_PDF), "background_info": DocumentExtractor.read_pdf(BACKGROUND_INFO_PDF), "editing_patterns": DocumentExtractor.read_pdf(EDITING_PATTERNS_PDF), "rr_i": DocumentExtractor.read_pdf(RR_I_PDF)}    def map_diagnostic_themes(self, source_texts: Dict[str, str]) -> str:        prompt = f"""You are assisting a qualified clinician with diagnostic-theme mapping for review. Map source evidence to diagnostically relevant themes for F88, F32.89, and F41.8. Do not quote DSM-5-TR text verbatim. Use only evidence present.SOURCE REPORT: {source_texts['verdana_report']}BACKGROUND: {source_texts['background_info']}"""        return self.model.generate_content(prompt).text    def revise_report(self, source_texts: Dict[str, str], diagnostic_mapping: str) -> str:        prompt = f"""Revise the psychological report from Referral Reason through Diagnostic Formulation. Bold all revised text. Avoid premature conceptualization. Build narrative climax. Integrate evidence for F88, F32.89, and F41.8 for clinician review.SOURCE REPORT: {source_texts['verdana_report']}FURTHER BACKGROUND: {source_texts['background_info']}EDITING PRINCIPLES: {source_texts['editing_patterns']}RR I: {source_texts['rr_i']}DIAGNOSTIC MAPPING: {diagnostic_mapping}"""        return self.model.generate_content(prompt).text    def summarize_revision(self, revised_report: str, diagnostic_mapping: str) -> str:        prompt = f"""Summarize how the revision avoided premature conceptualization, built narrative climax, and mapped evidence to F88, F32.89, and F41.8 for clinician review.MAPPING: {diagnostic_mapping}REPORT: {revised_report}"""        return self.model.generate_content(prompt).text    @staticmethod    def export_bolded_docx(revised_report: str, summary_text: str, output_path: Union[str, Path]) -> None:        doc = Document(); doc.styles["Normal"].font.name = "Times New Roman"; doc.styles["Normal"].font.size = Pt(11)        doc.add_heading("Summary of Editing Process and Diagnostic Integration", level=1)        for paragraph in summary_text.splitlines():            if paragraph.strip(): doc.add_paragraph(paragraph.strip())        doc.add_page_break(); doc.add_heading("Revised Psychological Report", level=1)        for paragraph in revised_report.splitlines():            clean = paragraph.strip()            if not clean: doc.add_paragraph(""); continue            p = doc.add_paragraph(); run = p.add_run(clean.replace("**", "")); run.bold = True        doc.save(output_path)    def run(self, output_path: Union[str, Path] = VERDANA_OUTPUT_DOCX) -> Dict[str, str]:        source_texts = self.load_source_texts()
        missing = [name for name, text in source_texts.items() if text.startswith("[FILE NOT FOUND")]        if missing: raise FileNotFoundError(f"Missing required PDFs: {missing}. Upload PDFs into /content first.")        diagnostic_mapping = self.map_diagnostic_themes(source_texts)        revised_report = self.revise_report(source_texts, diagnostic_mapping)        summary_text = self.summarize_revision(revised_report, diagnostic_mapping)        self.export_bolded_docx(revised_report, summary_text, output_path)        return {"diagnostic_mapping": diagnostic_mapping, "revised_report": revised_report, "summary": summary_text, "output_path": str(output_path)}def sieve_of_eratosthenes(n: int) -> List[int]:    if n < 2: return []    primes = [True] * (n + 1); primes[0] = False; primes[1] = False    for i in range(2, int(n ** 0.5) + 1):        if primes[i]:            for j in range(i * i, n + 1, i): primes[j] = False    return [i for i, is_prime in enumerate(primes) if is_prime]# Demo runnerdef main() -> None:    DocumentExtractor.extract_local_sources(AUTHORIZED_SOURCE_DIR)    if RUN_PUBLIC_WIKIPEDIA_SCRAPE: PublicMeasureScraper.save_scraped_data(AUTHORIZED_SOURCE_DIR)    library = SourceLibrary(); library.load_dir(AUTHORIZED_SOURCE_DIR)    SFTDatasetBuilder.build_jsonl(library, TRAIN_JSONL, VALIDATION_JSONL)    scorer = WAISScorer(); calculated = {idx: scorer.mock_composite_for_testing(idx, v) for idx, v in {"VCI":35,"PRI":20,"WMI":10,"PSI":25,"FSIQ":60}.items()}    print(WAISInterpreter().generate_narrative(calculated))    sample = ReportInput(client_name="Client", age=12, referral_question="Clarify cognitive strengths and weaknesses relevant to attention, executive functioning, learning, and diagnostic formulation.", behavioral_observations="Client was cooperative, though attention appeared variable and frustration increased during timed tasks.", diagnostic_questions=["ADHD", "Specific learning disorder", "Anxiety-related cognitive interference"], scores=[ScoreEntry(test_name="WAIS-IV", subtest_name="Digit Span", scaled_score=6), ScoreEntry(test_name="WAIS-IV", subtest_name="Coding", scaled_score=7), ScoreEntry(test_name="WAIS-IV", subtest_name="Similarities", scaled_score=11), ScoreEntry(test_name="D-KEFS", subtest_name="Trail Making Switching", scaled_score=5), ScoreEntry(test_name="CVLT-3", subtest_name="Delayed Recall", standard_score=85)])    engine = NeuropsychReportEngine(source_dir=AUTHORIZED_SOURCE_DIR); report = engine.run(sample, REPORT_DOCX)    print(sieve_of_eratosthenes(100)); print(report[:2000])    if RUN_GEMINI_REPORT_REVISION: VerdanaReportRevisionPipeline().run(output_path=VERDANA_OUTPUT_DOCX)if __name__ == "__main__":    main()